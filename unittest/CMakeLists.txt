INCLUDE_DIRECTORIES(${${PROJECT_NAME}_INCLUDE_DIR})

LIST(APPEND DIRS powerset )

FOREACH( D ${DIRS} )
    ADD_SUBDIRECTORY( ${D} )
ENDFOREACH( D )

ADD_EXECUTABLE(test_main test_main.cpp)
TARGET_LINK_LIBRARIES( test_main ${COMMON_LIBRARIES} powerset)
